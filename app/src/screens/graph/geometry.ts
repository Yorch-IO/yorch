/** Shared screen-space geometry for the interactive graph views. */
export const GRAPH_ZOOM = { MIN: 0.35, MAX: 6, STEP: 1.3 } as const;

/** Concepts are always 18px across, independently of mentions and zoom. */
export const CONCEPT_RADIUS = 9;
